# =============================================================================
# Git Activity Dashboard — Backend Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.
# Lines starting with '#' are comments and ignored.
# =============================================================================

# --- Database ----------------------------------------------------------------
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/git_dashboard

# --- Authentication / JWT ----------------------------------------------------
# A long random string used to sign JWT tokens.
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=change-me-to-a-random-secret

# Token lifetimes
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- Encryption (GitHub PAT storage) ----------------------------------------
# Fernet key for encrypting GitHub tokens at rest.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=change-me-to-a-fernet-key

# --- External APIs -----------------------------------------------------------
# Google Gemini API key for AI-powered code analysis.
GEMINI_API_KEY=your-gemini-api-key-here

# --- Localisation / Scheduling -----------------------------------------------
DEFAULT_TIMEZONE=Asia/Tokyo
SYNC_INTERVAL_HOURS=6
